# OPSD (On-Policy Self-Distillation) 复现开发者指南



## 1. 核心思想
OPSD 的核心动机是：如果一个大模型有足够的推理能力，且能看到标准答案或特权信息，它就能“理解

”并“解释”这些正确推理过程，从而可以作为“教师”来指导看不到答案的“自己”（学生）。

在 OPSD 框架中，不需要外部的、更大的教师模型或奖励模型 。教师策略和学生策略由同一个模型初始化而来，它们仅在输入的上下文条件上有所不同 ：
* **学生策略 $p_S$**：在推理和训练生成时，仅能看到问题 $x$。
* **教师策略 $p_T$**：作为特权信息的拥有者，不仅能看到问题 $x$，还能看到标准答案/参考思路 $y^*$ 。

## 2. 算法流程与目标函数
训练采用在线策略（On-Policy）采样，并提供密集的 token 级别反馈：
1.  **学生采样**：给定问题 $x$，学生策略生成一条完整的回复轨迹 $\hat{y} \sim p_S(\cdot|x)$ 。
2.  **教师和学生评估**：在生成的轨迹 $\hat{y}$ 的每一个 token 位置 $n$，教师（基于 $x, y^*$ 和历史生成 $\hat{y}_{<n}$）和学生（基于 $x$ 和历史生成 $\hat{y}_{<n}$）分别输出下一个 token 的全词表概率分布 。
3.  **计算散度（Loss）**：计算教师分布与学生分布在整条轨迹上的平均 Token 级别散度（如 JSD）。目标函数如下：
    $$D(p_T || p_S)(\hat{y}|x) \triangleq \frac{1}{|\hat{y}|}\sum_{n=1}^{|\hat{y}|}D(p_T(\cdot|x,y^*,\hat{y}_{<n}) || p_S(\cdot|x,\hat{y}_{<n}))$$ [cite: 174, 175]
4.  **梯度回传**：在计算图更新时，**梯度仅通过学生策略 $p_S$ 的 logits 回传**，教师策略 $p_T$ 仅作为固定目标提供监督信号 。

## 3. 实验基础设置
* **基础模型**：Qwen3 系列模型（1.7B, 4B, 8B 参数量），使用指令微调（instruct-tuned）版本 。
* **训练数据集**：Open Thoughts 数据集的数学推理子集。
* **数据量**：最高采样 30,000 条包含思维链（CoT）推理的问题-解决方案对 。
* **硬件支持**：8x A100 或 8x H100 GPUs 。
* **加速技术**：使用 bfloat16 混合精度、Flash Attention 2 以及梯度检查点（Gradient Checkpointing）以优化显存 。

## 4. 训练超参数与配置
以下是复现 OPSD 的关键训练参数设置：
* **优化器**：AdamW 。
* **散度度量 (Divergence Metric)**：使用全词表 Logit 蒸馏，具体采用广义 Jensen-Shannon 散度 $JSD_{\beta=0.5}$ 。
* **学习率 (Learning Rate)**：$2 \times 10^{-5}$ 。对于较短的生成长度（如 1024），使用较大的学习率 $2 \times 10^{-4}$ 效果更好 。
* **全局有效批次大小 (Effective Batch Size)**：32 （单卡 Batch Size 为 1，梯度累积步数为 4 ）。
* **采样策略 (Generations per Prompt)**：每个问题仅采样 1 个回复即可（相较于 GRPO 的 8 个，大幅节省成本） 。
* **最大生成长度 (Max Completion Length)**：1024 或 2048 token。论文的消融实验表明，增加到 2048 或 4096 能提供更多教师信号，显著提升性能 。

## 5. 关键复现细节（避坑指南）
* **冻结教师模型**：在训练过程中，作为目标的教师策略需要**固定为初始策略（Initial Policy）**，而不是正在更新的学习策略。这有助于稳定训练，并隐式起到正则化的作用，防止模型过度偏离初始状态 。
* **Prompt 模版的不一致性设计**：原论文在实现时采用了一种不对称的模板策略。教师的 Prompt 遵循了 Qwen3 带有思考标签（thinking tags）的完整聊天模板，而学生进行在线采样的 Prompt 故意去掉了聊天模板。这导致学生生成的是无模板风格的推理，而教师基于带 thinking 标签的格式给出分布 。虽然这并非强制要求，但这是论文报告性能的具体实现方式 。
* **教师零生成**：为了保证自然过渡，教师 prompt 会要求其生成新解法，但**在实际代码中教师模型不需要真正自回归生成 Token**，其“思考和合理化”过程是通过输入特权信息后的一次前向传播（Prefilling）隐式完成的。

## 6. 评估设置 (Evaluation)
为了对齐论文的 Benchmark 得分，评估时应采用一致的生成参数：
* **解码温度 (Temperature)**：1.0 
* **Top-p**：0.95 
* **最大生成长度 (Max New Tokens)**：38,912 
* **评估采样数 (Samples per Prompt)**：16 （用于计算 Pass@16） 
* **Thinking Mode**：开启 
* **评估频率**：每训练 100 步保存一个 Checkpoint 并进行评估，最高训练 1500 步，记录最佳分数。